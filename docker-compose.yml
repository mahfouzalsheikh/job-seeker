x-backend-env: &backend_env
  DATABASE_URL: postgresql://job_seeker:job_seeker@db:5432/job_seeker
  REDIS_URL: redis://redis:6379/0
  JOB_SEEKER_DATA_DIR: /app/data
  DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-dev-forth-secret-key-change-me}
  DJANGO_ALLOWED_HOSTS: "*"
  DJANGO_CSRF_TRUSTED_ORIGINS: ${DJANGO_CSRF_TRUSTED_ORIGINS:-http://localhost:8021,http://127.0.0.1:8021}
  OPENAI_TEXT_MODEL: ${OPENAI_TEXT_MODEL:-gpt-4.1-mini}
  OPENAI_EMBEDDING_MODEL: ${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}
  OPENAI_TIMEOUT_SECONDS: ${OPENAI_TIMEOUT_SECONDS:-20}
  OPENAI_MAX_RETRIES: ${OPENAI_MAX_RETRIES:-0}
  GOTENBERG_URL: http://gotenberg:3000
  SOURCE_HTTP_TIMEOUT_SECONDS: ${SOURCE_HTTP_TIMEOUT_SECONDS:-20}
  SOURCE_REFRESH_INTERVAL_SECONDS: ${SOURCE_REFRESH_INTERVAL_SECONDS:-21600}

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: job_seeker
      POSTGRES_USER: job_seeker
      POSTGRES_PASSWORD: job_seeker
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U job_seeker -d job_seeker"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  web:
    build:
      context: .
    image: forth:latest
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      gotenberg:
        condition: service_started
    environment:
      <<: *backend_env
      APP_ROLE: web
      DJANGO_SUPERUSER_USERNAME: admin
      DJANGO_SUPERUSER_PASSWORD: adminpass
      DJANGO_SUPERUSER_EMAIL: admin@example.com
    secrets:
      - openai_api_key
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  worker:
    image: forth:latest
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      web:
        condition: service_started
    environment:
      <<: *backend_env
      APP_ROLE: worker
      CELERY_WORKER_QUEUES: celery
      CELERY_WORKER_CONCURRENCY: ${CELERY_WORKER_CONCURRENCY:-2}
    secrets:
      - openai_api_key
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  beat:
    image: forth:latest
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      web:
        condition: service_started
    environment:
      <<: *backend_env
      APP_ROLE: beat
    secrets:
      - openai_api_key
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      web:
        condition: service_started
    ports:
      - "8021:80"
    volumes:
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./data:/app/data:ro
    restart: unless-stopped

  gotenberg:
    image: gotenberg/gotenberg:8
    command: ["gotenberg", "--api-timeout=60s", "--chromium-auto-start=true"]
    restart: unless-stopped

volumes:
  db_data:

secrets:
  openai_api_key:
    file: ./.secrets/openai_api_key
